BUGS:


TODO 

- General :
  > Diseño de clases
  

- Registra : 
  > Reloj de tiempo
  > Guada cada punto con su velocidad, distancia, altitud, y tiempo
  > Guada las sesiones con distancia , tiempo total y altitud acumulada
  > presenta el ritmo em min por segundo
  > mapa
  > adquiere un partial_wake_lock para que siga registrando (ver si funciona)
  > autopause con umbral de volocidad configurable y feeback en pantalla cuando activo
  > Presenta las altitudes acumuladas
  > al terminar la sesion se guarda en un GPX en la SD y se boran los puntos de la BBDD
  
  
  todo:
     > valorar si la velocidad que da el GPS no fluctua demasiado
     > que en el mapa de registro se vaya guardando el track
     > poder cargar tracks para seguirlos verlos en el mapa principal
     >  poder poner un track como activo en el mapa y que si me alejo mas de una distancia de el
        avise, o si voy en direccion contraria
     > soportar entrenamiento por intervalos, registrando los ritmos de cada intervalo
     
           el diseño de esto es el siguiente
           en la activity de registra poner un menu que lance un activity donde se gestionan 
           los intervalos, se crean, etc  y se selecciona 
           esta activity devuelve como result un intent con un array con cada uno de los intervalos
           ejem, si es 2x1' con 30'' de descanso y 1' de preparacion, devuelve { {preparacion,60000,ms}, {corre, 60000, ms}, {trota, 30000,ms},{corre,60000,ms}, {trota,30000,ms}}
           de esta forma queda preparado tambien para intervalos con distancia
           ejem si es 2x300m con 30'' descanso y 1' de preparacion { {preparacion,60000,ms}, {corre, 300, m}, {trota, 30000,ms},{corre,300,m}, {trota,30000,ms}}
     	   
     	   la activity registro cambia y pone en otro color los marcadores de los intervalos (cada vez uno si cambia de dist a tiempo )
     	   y van a ir contando haci a atras (pensar si esto lo gestiona la acitvity de registro o el servicio)
     	   
     	   luego al dar a estart, se le pasa tambien el array a el servicio que es el que
     	   se encarga de hacer ruidos cada vez que pasa por cada una de las condiciones que marca el array
     	   asi como de poner el label y el flag de intervalos en el punto
     	   
     	 
     
     
     > solo registra un punto cuando esta mas lejos que la acuracy del anterior
     > al dar a parar, que salga un resumen general de la sesion y botones para grabar o eliminar direcatmente
          llamar al activity de puntossesion
     > seleccionar el deporte antes de empezar a registrar
     

- Listas
  > Saca lista de sesiones
  > sacar tabla de sesion (puntos en la sesion), una fila por km
  > poder borrar una sesion 
  > sacar mapa
  > exportar el track a KML
  > el tabla de km indicaciones de como de subida/bajada tiene un tramo
  > en mapa, tramos por km con etiqueta de ritmo y botones para ir de un km a otro
  > lista de km, tiempo, sesion y altitud por semana, mes y año
  > las altitues acumuladas en las cabeceras
  > poder meter un campo texto descripcion de la sesion y editarlo
  > es posible añadir sesiones  no registradas
  > las sesiones se guardan como TRX, no hace falta exportar
  > poder marcar sesion como destacada
  > en las lista de sesiones poder pasar de entre la de todas a la de destacadas
  todo:
     > varios gráficos...
     > si tengo laps cargadas, sacar tambien una lista por lap
     > cargar track en KML para sesiones no registradas 
     > poder asociar una sesion a otra que ester marcada como hito, para asi poder comparar sesiones
     > en lista de sesiones asociadas, poder cambiar de entre ellas cual es el hito sin perder la asociacion
    
     
     
     
- Config
  > se pueden configurar cada cuanto tiempo y distancia se activa el GPS
  > se puede configurar el autopause y su umbral
  > boton para volver a config por defecto

  todo:
     > meter lista para tener varias config, y que luego se elijan al registrar
     > según el modo se configura la pantalal principal del regisra
       - correr : velocidad en mi por km mas grande
       - bici : km/h ,as grande
       - andar, saco mapa y waypoints solo a peticion
       

- Pruebas:
  >
  > revisar la navegación entre pantallas, en espacial registrando y al volver de la notificacion
  
- conclusiones
 > para correr una config de cada 10m y 6 seg esta bien 
  
  